import Navbar from '../../nav/Navbar';
import { StyledHeader } from './Header.styled';

const Header = () => {
    return (
        <StyledHeader>
            <Navbar />
        </StyledHeader>
    )
}

export default Header